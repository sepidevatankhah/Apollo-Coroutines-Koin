query getAllPosts(
  $options: PageQueryOptions
) {
  posts(options: $options) {

    data {
      user{
        id
        name
        username
        email
        phone
      }
      id
      title
      body
    }
    meta {
      totalCount
    }
  }
}

