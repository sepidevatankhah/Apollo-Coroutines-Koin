query getAllPosts($options: PageQueryOptions) {
  posts(options: $options) {

    data {
      user{
        id
        name
        username
      }
      id
      title
      body
    }
    meta {
      totalCount
    }
  }
}

